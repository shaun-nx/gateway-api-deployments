apiVersion: v1
kind: Namespace
metadata:
  name: gateway-reqhdr-example
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello
  namespace: gateway-reqhdr-example
  labels:
    app: hello
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello
  template:
    metadata:
      labels:
        app: hello
    spec:
      containers:
        - name: hello
          image: nginxdemos/hello:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: hello
  namespace: gateway-reqhdr-example
  labels:
    app: hello
spec:
  selector:
    app: hello
  ports:
    - name: http
      port: 80
      targetPort: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: hello-reqhdr-gw
  namespace: gateway-reqhdr-example
spec:
  gatewayClassName: nginx
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: reqhdr.127.0.0.1.nip.io
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hello-reqhdr-route
  namespace: gateway-reqhdr-example
spec:
  parentRefs:
    - name: hello-reqhdr-gw
  hostnames:
    - reqhdr.127.0.0.1.nip.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: RequestHeaderModifier
          requestHeaderModifier:
            add:
              - name: X-From-Gateway
                value: "true"
      backendRefs:
        - name: hello
          port: 80
